# حالت توسعه (Development Mode)

## استفاده

برای فعال/غیرفعال کردن حالت توسعه، فایل زیر را ویرایش کنید:

```
config/dev.js
```

## تنظیمات

### فعال کردن حالت توسعه (بدون نیاز به سرور)
```javascript
export const DEV_MODE = true;
```

در این حالت:
- ✅ API واقعی فراخوانی نمی‌شود
- ✅ از داده‌های فیک استفاده می‌شود
- ✅ می‌توانید بدون سرور تست کنید
- ✅ هر نام کاربری و شماره‌ای قبول می‌شود
- ✅ هر کد OTP قبول می‌شود

### غیرفعال کردن حالت توسعه (استفاده از API واقعی)
```javascript
export const DEV_MODE = false;
```

در این حالت:
- ✅ API واقعی فراخوانی می‌شود
- ✅ نیاز به سرور فعال دارید
- ✅ اعتبارسنجی واقعی انجام می‌شود

## داده‌های فیک

داده‌های فیک در `config/dev.js` قابل تنظیم هستند:

```javascript
export const MOCK_DATA = {
  login: {
    success: true,
    message: "کد تایید برای شما ارسال شد",
  },
  verify: {
    success: true,
    user: { ... }
  }
};
```

## تاخیر شبیه‌سازی شده

برای شبیه‌سازی واقعی‌تر، یک تاخیر 1 ثانیه‌ای اضافه شده است:

```javascript
export const MOCK_DELAY = 1000; // میلی‌ثانیه
```

## نکات مهم

⚠️ **قبل از ارسال به production، حتماً `DEV_MODE` را `false` کنید!**

💡 در حالت توسعه، پیام‌های console با 🔧 مشخص می‌شوند.
